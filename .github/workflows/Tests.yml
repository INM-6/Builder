name: Tests
on: [push, pull_request]
jobs:
  bats:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install BATS
      run: |
        ./build.sh configure || true
        yes | ./build bats
    - name: Run BATS
      run: bats tests/
